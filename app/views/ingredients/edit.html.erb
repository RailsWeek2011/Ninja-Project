 <%= mininav(["Zutaten", (link_to "Kategorien", ingredient_categories_path)] +
              getCategory(@ingredient) + [(link_to @ingredient.name, ingredient_path(@ingredient)), "Bearbeiten"]) %>

<br />

<%= render 'form' %>

<%= link_to 'Show', @ingredient %>
