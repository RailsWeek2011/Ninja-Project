<%= mininav(["Rezepte", "Gerichte"]) %>
<br/>
<table border="1">
  <tr>
    <th>Name</th>
    <th>Description</th>
  </tr>

<% @dishes.each do |dish| %>
  <tr>
    <td><%= link_to dish.name, dish_path(dish) %></td>
    <td><%= dish.description %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Neues Gericht', new_dish_path %>
<%= link_to 'Neues Rezept', new_recipe_path %>
